#!/bin/bash
. ${0%/*}/ENV.setview   # Environment

querysql=<<EOT
select *, timeend-timestart duration
from timelog
where 
     username = '${LOGNAME:-$USERNAME}'
order by viewname, timestart 
EOT

   mysql -h localhost -u ${1:-viewtime} -p${2:admin} viewtime <<EOT
$querysql
EOT
